# servers.yml

.base: &base
  user: john
  branch: master
  theme_name: twentytwenty
  deploy_path: "/var/www/html"

production-blog:
  <<: *base
  stage: production
  branch: master

development-blog:
  <<: *base
  stage: staging
  branch: develop
